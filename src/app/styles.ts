import styled from '@emotion/styled';
import { gallery } from '../emotion/colors';

export const AppContainer = styled('div')`
  display: flex;
  flex-direction: row;
  width: 100%;
  height: 100%;
  background-color: ${gallery};
`;
