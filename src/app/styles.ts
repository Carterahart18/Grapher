import styled from '@emotion/styled';
import { galleryWhite } from '../emotion/colors';

export const AppContainer = styled('div')`
  width: 100vw;
  height: 100vh;
  background-color: ${galleryWhite};
`;
