import styled from '@emotion/styled';
import { silverChalice, white } from '../../emotion/colors';

export const Canvas = styled('canvas')`
  border: 2px solid ${silverChalice};
  border-radius: 2px;
  background-color: ${white};
`;
